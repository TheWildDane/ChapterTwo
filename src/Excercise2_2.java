import java.util.Scanner;
import java.lang.Math;
import java.util.*;

public class Excercise2_2 {
    // Compute the volume of a triangle)
    // Write a program that reads in the length of sides of an equilateral triangle and computes the area and volume
    // using the following formulas:
    // area = Math.sqrt(3) / 4 (length of sides)^2
    // volume = area * length

    Scanner Input = new Scanner(System.in);
    double length = Input.nextDouble();
    double pow2 = Math.pow(length, 2);

    double area = (Math.sqrt(3)/4);


}
